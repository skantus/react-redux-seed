export const styles = theme => ({
  container: {
    display: 'flex',
    flexDirection: 'column'
  },
  input: {
    margin: theme.spacing.unit
  },
  button: {
    margin: theme.spacing.unit * 2
  }
});
